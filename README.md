# itmo_cv_course

В этой ветке содержится решение 4-его ДЗ по курсу CV. Задача данного ДЗ состоит в реализации двух алгоритмов трекинга перемещающихся объектов по их bbox'ам - tracker_soft и tracker_strong. В качестве объектов детекции выступают кантриболы (кол-во опционально), для каждого их которых генерируется путь с bbox'ом (возможно пустой или сдвинутый, с обозначенной вероятностью).

Необходимо выбрать целевые метрики оценки качества и замерить оба реализованных метода на разных конфигурациях кантриболов.

Все задачи успешно решены, исходный код решения предоставлен fastapi_server.py

## Tracker soft

Реализация начинается с имплементации tracker_soft, основанного на евклидовом расстоянии между центрами bbox'ов объектов, выбранного из-за его низкой вычислительной сложности, что соответствует ограничению на использование минимальных ресурсов. 

На каждом кадре вычисляется матрица стоимостей (cost_matrix), содержащая расстояния между центрами активных треков с предыдущего шага и центрами текущих обнаружений. Затем применяется венгерский алгоритм для оптимального сопоставления треков и обнаружений на основе вычисленных расстояний, отбрасывая пары с расстоянием выше заданного порога. Успешно сопоставленные треки обновляют свое положение и сбрасывают счетчик пропущенных кадров, при этом соответствующим обнаружениям присваивается существующий идентификатор трека. Для ненайденных треков увеличивается счетчик пропущенных кадров, и трек удаляется при превышении порога MAX_AGE_SOFT. Обнаружения, не нашедшие соответствия среди активных треков, инициализируют новые треки с уникальными идентификаторами.

Подход показывает крайне высокую скорость работы, а также достаточное качество (см. эксперименты).

## Tracker strong

Усовершенствуем Tracker soft добавлением IoU для сопоставления, что должно обеспечить более надежное отслеживание при перекрытии объектов по сравнению с простым расстоянием. Для каждого кадра, помимо евклидовых расстояниий, вычисляется матрица стоимостей на основе IoU (стоимость равна 1 минус IoU) между последними известными положениями активных треков и текущими обнаружениями.

Оптимальное назначение обнаружений трекам также находится с помощью венгерского алгоритма, при этом учитываются только пары с IoU, превышающим заданный порог IOU_THRESHOLD_STRONG и DISTANCE_THRESHOLD_STRONG. Сопоставленные треки обновляют свое положение, сбрасывают счетчик пропущенных кадров и увеличивают счетчик успешных обнаружений (hits), а детекциям присваивается соответствующий ID трека. Несопоставленные треки увеличивают свой возраст и удаляются при превышении MAX_AGE_STRONG, в то время как несопоставленные обнаружения инициируют создание новых треков с начальным счетчиком hits равным единице.

Данный подход показал сниженное качество работы относительно soft (см. эксперименты).

## Метрики

Для оценки качества реализованных трекеров вычисляются две ключевые метрики на основе сравнения присвоенных track_id с истинными cb_id:
- Метрика ID Switches подсчитывает количество случаев, когда одному и тому же реальному объекту (cb_id), видимому на двух последовательных кадрах, были присвоены разные идентификаторы трека (track_id), отражая стабильность присвоения ID. Ниже - лучше
- Метрика Average Fragments Per Track измеряет среднее число уникальных track_id, полученных одним реальным объектом (cb_id) за все время его существования в видео, характеризуя непрерывность отслеживания. Ниже - лучше

Обе метрики используют ground truth данные (cb_id) для вычислений уже после работы трекера. Низкие значения обеих метрик указывают на более высокое качество отслеживания, стремясь к нулю для ID Switches и к единице для Average Fragments. Эти показатели позволяют количественно сравнить эффективность tracker_soft и tracker_strong в различных условиях.

Оценка алгоритмов проводилась на следующих конфигурациях данных:
- tracks_amount = 5, random_range = 10, bb_skip_percent = .5
- tracks_amount = 10, random_range = 5, bb_skip_percent = .25
- tracks_amount = 20, random_range = 3, bb_skip_percent = .1                                                                                          |

## Эксперименты

Было проведено несколько экспериментов с tracker_soft/strong на 3-ёх версиях данных:

| Количество Кантриболов | Трекер         | ID Switches | Average Fragments Per Track |
|-------------------------|----------------|-------------|-----------------------------|
| 5                       | tracker_soft   | 1           | 4.0                         |
|                         | tracker_strong | 11          | 9.0                         |
| 10                      | tracker_soft   | 10          | 3.2                         |
|                         | tracker_strong | 38          | 9.5                         |
| 20                      | tracker_soft   | 27          | 2.65                        |
|                         | tracker_strong | 69          | 5.4                         |

Полученные метрики показывают, что tracker_soft последовательно демонстрирует лучшую производительность по сравнению с tracker_strong во всех протестированных сценариях, имея значительно меньшее количество переключений ID и более низкую фрагментацию треков. Увеличение количества объектов приводит к ухудшению показателей обоих трекеров, однако tracker_soft показывает меньшую степень деградации метрик.

## Выводы

В рамках данной работы были успешно реализованы два алгоритма отслеживания объектов (tracker_soft и tracker_strong) и проведена их оценка с использованием метрик ID Switches и Average Fragments Per Track. Эксперименты показали, что более простой tracker_soft, основанный на евклидовом расстоянии, стабильно превосходит tracker_strong, комбинирующий IoU и расстояние, что может быть связано с чувствительностью IoU к шуму в координатах bbox'ов или неоптимальным подбором гиперпараметров для tracker_strong.

Для дальнейшего улучшения качества отслеживания, особенно для tracker_strong, можно рассмотреть следующие направления:
- Внедрение модели движения: Использование фильтра Калмана для предсказания положения объектов на следующем кадре может значительно повысить точность ассоциации, особенно при пропусках детекций или резких маневрах.
- Более гибкое управление треками: Реализация каскадного сопоставления (отдавая приоритет более "уверенным" трекам) и введение различных MAX_AGE для подтвержденных и предварительных треков может повысить робастность tracker_strong.
- Адаптивные пороги: Возможность динамической подстройки порогов (DISTANCE_THRESHOLD, IOU_THRESHOLD) в зависимости от характеристик движения объекта или плотности объектов в кадре.
